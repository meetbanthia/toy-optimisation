This is a toy example to demonstrate RT core optimisation.

Current state of the repo shows fetched 3dgrt code from vk_gaussian_splatting opensource project by nvcore-samples. This will the baseline used by me to test theoretical optimisation on rendering framework. Max speedup expected is ~1.3. Vulkan is required to test the optimisation, as it gives more lower level control.
